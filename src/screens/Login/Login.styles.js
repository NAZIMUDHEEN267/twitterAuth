import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    img: {
        height: 45,
        width: 45,
        elevation: 40,
    },
    text_sm: {
        marginTop: 15,
        color: "#544"
    }
});

export default styles;